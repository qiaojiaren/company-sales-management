<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.InventoryMapper">
    <insert id="insert">
        insert into inventory (product_name, quantity, sell_price,
                               purchase_price, threshold, create_time, update_time)
        values (#{productName},#{quantity},#{sellPrice},
                #{purchasePrice},#{threshold},#{createTime},#{updateTime})
    </insert>
</mapper>